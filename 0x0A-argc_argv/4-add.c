#include <stdio.h>
#include <stdlib.h>
/**
* main - Entry point
* @argc: int
* @argv: array
*
* Return: Always 0 (Succes)
*/
int main(int argc, char *argv[])
{
	int i, add = 0;

	if(argc > 1)
	{
		for (i = 0; i < argc; i++)
		{
			if (argv[i] >= 0)
			{
				add = add + 
		value = atoi(argv[1]);
		printf("%d\n", value); 
	}
	else
	{
		print("0\n");
	}
	return (0);
}
