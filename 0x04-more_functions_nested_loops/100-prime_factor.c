#include <stdio.h>
/**
 * main - printing numbers
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	int i, j, k;

	for (i = 2; i <=50829599; i++)
	{
		for (j = 0; j <= i; i++)
		{
			
		if (!(i % k))
		{
			
		}
	}

	printf("\n");

	return (0);
}
